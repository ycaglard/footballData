package com.example.smemo.Enum;

public enum Type {
    SCORE, //if its a score format that contains home and away teams score such as 0-0
    PERCENTAGE,
    NAME,
    COUNT// if its just a goal count etc
}
